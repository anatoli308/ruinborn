[package]
name = "tradewars-game"
version = "0.1.0"
edition = "2021"
description = "TradeWars shared game logic — server-authoritative simulation"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.8"
