[package]
name = "ruinborn-protocol"
version = "0.1.0"
edition = "2021"
description = "Ruinborn network protocol — shared message types between server and client"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ruinborn-game = { path = "../ruinborn-game" }
